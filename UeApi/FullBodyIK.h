#pragma once
/*
Package /Script/FullBodyIK, generated by AssetGen/tools/genueapi.py. Do not edit.

A member is here if and only if AssetGen can compile a use of it.
*/
#include "UeMeta.h"
#include "ControlRig.h"
#include "CoreUObject.h"

enum class EFBIKBoneLimitType : uint8
{
    Free                   = 0,
    Limit                  = 1,
    Locked                 = 2,
    EFBIKBoneLimitType_MAX = 3,
};

enum class EPoleVectorOption : uint8
{
    Direction             = 0,
    Location              = 1,
    EPoleVectorOption_MAX = 2,
};

struct FFBIKBoneLimit
{
    EFBIKBoneLimitType LimitType_X;
    EFBIKBoneLimitType LimitType_Y;
    EFBIKBoneLimitType LimitType_Z;
    FVector Limit;

    FFBIKBoneLimit() = default;
    FFBIKBoneLimit(EFBIKBoneLimitType LimitType_X, EFBIKBoneLimitType LimitType_Y, EFBIKBoneLimitType LimitType_Z, FVector Limit) {}
};

struct FFBIKConstraintOption
{
    FRigElementKey Item;
    bool bEnabled;
    bool bUseStiffness;
    FVector LinearStiffness;
    FVector AngularStiffness;
    bool bUseAngularLimit;
    FFBIKBoneLimit AngularLimit;
    bool bUsePoleVector;
    EPoleVectorOption PoleVectorOption;
    FVector PoleVector;
    FRotator OffsetRotation;

    FFBIKConstraintOption() = default;
    FFBIKConstraintOption(FRigElementKey Item, bool bEnabled, bool bUseStiffness, FVector LinearStiffness, FVector AngularStiffness, bool bUseAngularLimit, FFBIKBoneLimit AngularLimit, bool bUsePoleVector, EPoleVectorOption PoleVectorOption, FVector PoleVector, FRotator OffsetRotation) {}
};

struct FFBIKDebugOption
{
    bool bDrawDebugHierarchy;
    bool bColorAngularMotionStrength;
    bool bColorLinearMotionStrength;
    bool bDrawDebugAxes;
    bool bDrawDebugEffector;
    bool bDrawDebugConstraints;
    FTransform DrawWorldOffset;
    float DrawSize;

    FFBIKDebugOption() = default;
    FFBIKDebugOption(bool bDrawDebugHierarchy, bool bColorAngularMotionStrength, bool bColorLinearMotionStrength, bool bDrawDebugAxes, bool bDrawDebugEffector, bool bDrawDebugConstraints, FTransform DrawWorldOffset, float DrawSize) {}
};

struct FFBIKEndEffector
{
    FRigElementKey Item;
    FVector Position;
    float PositionAlpha;
    int PositionDepth;
    FQuat Rotation;
    float RotationAlpha;
    int RotationDepth;
    float Pull;

    FFBIKEndEffector() = default;
    FFBIKEndEffector(FRigElementKey Item, FVector Position, float PositionAlpha, int PositionDepth, FQuat Rotation, float RotationAlpha, int RotationDepth, float Pull) {}
};

struct FMotionProcessInput
{
    bool bForceEffectorRotationTarget;
    bool bOnlyApplyWhenReachedToTarget;

    FMotionProcessInput() = default;
    FMotionProcessInput(bool bForceEffectorRotationTarget, bool bOnlyApplyWhenReachedToTarget) {}
};

struct FSolverInput
{
    float LinearMotionStrength;
    float MinLinearMotionStrength;
    float AngularMotionStrength;
    float MinAngularMotionStrength;
    float DefaultTargetClamp;
    float Precision;
    float Damping;
    int MaxIterations;
    bool bUseJacobianTranspose;

    FSolverInput() = default;
    FSolverInput(float LinearMotionStrength, float MinLinearMotionStrength, float AngularMotionStrength, float MinAngularMotionStrength, float DefaultTargetClamp, float Precision, float Damping, int MaxIterations, bool bUseJacobianTranspose) {}
};

struct FRigUnit_FullbodyIK_WorkData
{
};

struct FRigUnit_FullbodyIK : public FRigUnit_HighlevelBaseMutable
{
    FRigElementKey Root;
    TArray<FFBIKEndEffector> Effectors;
    TArray<FFBIKConstraintOption> Constraints;
    FSolverInput SolverProperty;
    FMotionProcessInput MotionProperty;
    bool bPropagateToChildren;
    FFBIKDebugOption DebugOption;
    FRigUnit_FullbodyIK_WorkData WorkData;
};
