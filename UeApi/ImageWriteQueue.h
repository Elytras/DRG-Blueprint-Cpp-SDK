#pragma once
/*
Package /Script/ImageWriteQueue, generated by AssetGen/tools/genueapi.py. Do not edit.

A member is here if and only if AssetGen can compile a use of it.
*/
#include "UeMeta.h"
#include "Engine.h"

class UTexture;

enum class EDesiredImageFormat : uint8
{
    PNG                     = 0,
    JPG                     = 1,
    BMP                     = 2,
    EXR                     = 3,
    EDesiredImageFormat_MAX = 4,
};

struct FImageWriteOptions
{
    EDesiredImageFormat Format;
    int CompressionQuality;
    bool bOverwriteFile;
    bool bAsync;
};

class UImageWriteBlueprintLibrary : public UBlueprintFunctionLibrary
{
public:
    UE_CLASS("/Script/ImageWriteQueue", "ImageWriteBlueprintLibrary");
    static void ExportToDisk(class UTexture* Texture, FString Filename, const FImageWriteOptions& options);
};
