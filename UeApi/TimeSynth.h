#pragma once
/*
Package /Script/TimeSynth, generated by AssetGen/tools/genueapi.py. Do not edit.

A member is here if and only if AssetGen can compile a use of it.
*/
#include "UeMeta.h"
#include "AudioMixer.h"
#include "CoreUObject.h"

enum class ETimeSynthBeatDivision : uint8
{
    One                        = 0,
    Two                        = 1,
    Four                       = 2,
    Eight                      = 3,
    Sixteen                    = 4,
    Count                      = 5,
    ETimeSynthBeatDivision_MAX = 6,
};

enum class ETimeSynthEnvelopeFollowerPeakMode : uint8
{
    MeanSquared                            = 0,
    RootMeanSquared                        = 1,
    Peak                                   = 2,
    Count                                  = 3,
    ETimeSynthEnvelopeFollowerPeakMode_MAX = 4,
};

enum class ETimeSynthEventClipQuantization : uint8
{
    Global                              = 0,
    None                                = 1,
    Bars8                               = 2,
    Bars4                               = 3,
    Bars2                               = 4,
    Bar                                 = 5,
    HalfNote                            = 6,
    HalfNoteTriplet                     = 7,
    QuarterNote                         = 8,
    QuarterNoteTriplet                  = 9,
    EighthNote                          = 10,
    EighthNoteTriplet                   = 11,
    SixteenthNote                       = 12,
    SixteenthNoteTriplet                = 13,
    ThirtySecondNote                    = 14,
    Count                               = 15,
    ETimeSynthEventClipQuantization_MAX = 16,
};

enum class ETimeSynthEventQuantization : uint8
{
    None                            = 0,
    Bars8                           = 1,
    Bars4                           = 2,
    Bars2                           = 3,
    Bar                             = 4,
    HalfNote                        = 5,
    HalfNoteTriplet                 = 6,
    QuarterNote                     = 7,
    QuarterNoteTriplet              = 8,
    EighthNote                      = 9,
    EighthNoteTriplet               = 10,
    SixteenthNote                   = 11,
    SixteenthNoteTriplet            = 12,
    ThirtySecondNote                = 13,
    Count                           = 14,
    ETimeSynthEventQuantization_MAX = 15,
};

enum class ETimeSynthFFTSize : uint8
{
    Min_64                = 0,
    Small_256             = 1,
    Medium_512            = 2,
    Large_1024            = 3,
    ETimeSynthFFTSize_MAX = 4,
};

enum class ETimeSynthFilter : uint8
{
    FilterA              = 0,
    FilterB              = 1,
    Count                = 2,
    ETimeSynthFilter_MAX = 3,
};

enum class ETimeSynthFilterType : uint8
{
    LowPass                  = 0,
    HighPass                 = 1,
    BandPass                 = 2,
    BandStop                 = 3,
    Count                    = 4,
    ETimeSynthFilterType_MAX = 5,
};

struct FTimeSynthClipHandle
{
    FName ClipName;
    int ClipId;

    FTimeSynthClipHandle() = default;
    FTimeSynthClipHandle(FName ClipName, int ClipId) {}
};

struct FTimeSynthClipSound
{
    class USoundWave* SoundWave;
    float RandomWeight;
    FVector2D DistanceRange;

    FTimeSynthClipSound() = default;
    FTimeSynthClipSound(class USoundWave* SoundWave, float RandomWeight, FVector2D DistanceRange) {}
};

struct FTimeSynthEnvelopeFollowerSettings
{
    float AttackTime;
    float ReleaseTime;
    ETimeSynthEnvelopeFollowerPeakMode PeakMode;
    bool bIsAnalogMode;

    FTimeSynthEnvelopeFollowerSettings() = default;
    FTimeSynthEnvelopeFollowerSettings(float AttackTime, float ReleaseTime, ETimeSynthEnvelopeFollowerPeakMode PeakMode, bool bIsAnalogMode) {}
};

struct FTimeSynthFilterSettings
{
    ETimeSynthFilterType FilterType;
    float CutoffFrequency;
    float FilterQ;

    FTimeSynthFilterSettings() = default;
    FTimeSynthFilterSettings(ETimeSynthFilterType FilterType, float CutoffFrequency, float FilterQ) {}
};

struct FTimeSynthQuantizationSettings
{
    float BeatsPerMinute;
    int BeatsPerBar;
    ETimeSynthBeatDivision BeatDivision;
    float EventDelaySeconds;
    ETimeSynthEventQuantization GlobalQuantization;

    FTimeSynthQuantizationSettings() = default;
    FTimeSynthQuantizationSettings(float BeatsPerMinute, int BeatsPerBar, ETimeSynthBeatDivision BeatDivision, float EventDelaySeconds, ETimeSynthEventQuantization GlobalQuantization) {}
};

struct FTimeSynthSpectralData
{
    float FrequencyHz;
    float Magnitude;

    FTimeSynthSpectralData() = default;
    FTimeSynthSpectralData(float FrequencyHz, float Magnitude) {}
};

struct FTimeSynthTimeDef
{
    int NumBars;
    int NumBeats;

    FTimeSynthTimeDef() = default;
    FTimeSynthTimeDef(int NumBars, int NumBeats) {}
};

class UTimeSynthVolumeGroup : public UObject
{
public:
    UE_CLASS("/Script/TimeSynth", "TimeSynthVolumeGroup");
    float DefaultVolume;
};

class UTimeSynthClip : public UObject
{
public:
    UE_CLASS("/Script/TimeSynth", "TimeSynthClip");
    TArray<FTimeSynthClipSound> Sounds;
    FVector2D VolumeScaleDb;
    FVector2D PitchScaleSemitones;
    FTimeSynthTimeDef FadeInTime;
    bool bApplyFadeOut;
    FTimeSynthTimeDef FadeOutTime;
    FTimeSynthTimeDef ClipDuration;
    ETimeSynthEventClipQuantization ClipQuantization;
};

class UTimeSynthComponent : public USynthComponent
{
public:
    UE_CLASS("/Script/TimeSynth", "TimeSynthComponent");
    FTimeSynthQuantizationSettings QuantizationSettings;
    bool bEnableSpectralAnalysis;
    TArray<float> FrequenciesToAnalyze;
    ETimeSynthFFTSize FFTSize;
    TMulticastInlineDelegate<void(float SynthPlaybackTimeSeconds)> OnPlaybackTime;
    bool bIsFilterAEnabled;
    bool bIsFilterBEnabled;
    FTimeSynthFilterSettings FilterASettings;
    FTimeSynthFilterSettings FilterBSettings;
    bool bIsEnvelopeFollowerEnabled;
    FTimeSynthEnvelopeFollowerSettings EnvelopeFollowerSettings;
    int MaxPoolSize;
    void AddQuantizationEventDelegate(ETimeSynthEventQuantization QuantizationType, TDelegate<void(ETimeSynthEventQuantization QuantizationType, int NumBars, float Beat)> OnQuantizationEvent);
    bool HasActiveClips();
    FTimeSynthClipHandle PlayClip(class UTimeSynthClip* InClip, class UTimeSynthVolumeGroup* InVolumeGroup);
    void ResetSeed();
    void SetBPM(float BeatsPerMinute);
    void SetEnvelopeFollowerEnabled(bool bInIsEnabled);
    void SetEnvelopeFollowerSettings(FTimeSynthEnvelopeFollowerSettings InSettings);
    void SetFFTSize(ETimeSynthFFTSize InFFTSize);
    void SetFilterEnabled(ETimeSynthFilter Filter, bool bIsEnabled);
    void SetFilterSettings(ETimeSynthFilter Filter, FTimeSynthFilterSettings InSettings);
    void SetQuantizationSettings(FTimeSynthQuantizationSettings InQuantizationSettings);
    void SetSeed(int InSeed);
    void SetVolumeGroup(class UTimeSynthVolumeGroup* InVolumeGroup, float VolumeDb, float FadeTimeSec);
    void StopClip(FTimeSynthClipHandle InClipHandle, ETimeSynthEventClipQuantization EventQuantization);
    void StopClipWithFadeOverride(FTimeSynthClipHandle InClipHandle, ETimeSynthEventClipQuantization EventQuantization, FTimeSynthTimeDef FadeTime);
    void StopSoundsOnVolumeGroup(class UTimeSynthVolumeGroup* InVolumeGroup, ETimeSynthEventClipQuantization EventQuantization);
    void StopSoundsOnVolumeGroupWithFadeOverride(class UTimeSynthVolumeGroup* InVolumeGroup, ETimeSynthEventClipQuantization EventQuantization, FTimeSynthTimeDef FadeTime);
    UE_PURE int GetBPM() const;
    UE_PURE float GetEnvelopeFollowerValue() const;
    UE_PURE int GetMaxActiveClipLimit() const;
    UE_PURE TArray<FTimeSynthSpectralData> GetSpectralData() const;
};
